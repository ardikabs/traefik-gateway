http:
  routers:
    kuma:
      entryPoints:
        - websecure
      rule: "Host(`kuma.local0.ardikabs.com`)"
      service: kuma@docker
      priority: 10
      tls:
        certResolver: cloudflare
        domains:
          - main: "kuma.local0.ardikabs.com"

